"""Prospect API endpoint stub.

NOTE (Agent5): ``app/api/__init__.py`` imports this module by name. The full
implementation belongs to the prospects agent. This stub keeps the import
chain healthy so the application can boot and unrelated tests can run.
It should be replaced by the owning agent's real implementation.
"""

# Intentionally left empty - presence of the module is the contract.
